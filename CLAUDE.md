# プロジェクト仕様書

Roblox上の多チーム戦術バトル。
3人×4チーム（初版：2人×3チーム）。リスポーンなし・1試合1命。
常時全体レーダー環境下で、装備と体力の配分を判断しながら戦う。

**設計の核：チームで行動して相手を倒すこと。**
全ての仕様はこの一点を支えるために存在する。核に反する変更を加えないこと。

---

## 1. 試合形式

- 1試合のみ。ラウンドは存在しない
- リスポーン・蘇生ともに実装しない
- 終了条件：
  - 生存チームが1チーム以下になる
  - 制限時間（上限5分）の経過
- 4チームの得点を画面上部に常時表示

### 得点
| 事象 | 点 |
|---|---|
| 敵の撃破 | 1点 |
| 自チームのみ生存 | +2点 |
| 完走ボーナス | +1点 |
| 自滅・場外 | 0点（誰にも入らない） |

**時間切れで決着しない場合、生存ボーナスは与えない。**
理由：待機して生き残ることに価値を持たせないため。
得点手段を撃破のみに限定することで、膠着戦術を成立させない。

### 撃破点の帰属
- 止めを刺した者に帰属する
- 継続ダメージによる死亡の場合は、最も多くダメージを与えた者
- 横取りは実力不足として許容する（仕様である）

### ランキング
- 累計方式。1試合の得点がそのまま累計に加算される
- ただし1試合あたり2点の参加コストを差し引く
- 累計は0未満にしない

理由：単純加算だと「下手でも長時間プレイした者が1位」となる。
参加コスト方式なら試合数を重ねる動機を残しつつ実力が反映される。

### 死亡後
- 観測モードへ移行。味方視点の切替とPing送信が可能
- Pingが味方の得点に貢献できることをUIで明示する
- 離脱ボタンは目立たせない

---

## 2. 体力（単一リソース）

体力バーは1本のみ。以下の全てに使用される。

1. **装備コスト**：装備したスロット数に応じて最大体力が減少
2. **使用コスト**：武器の使用ごとに減少
3. **維持コスト**：ステルス等が毎秒減少させる
4. **被弾ダメージ**

- 試合中の回復なし
- 残り体力が使用コストを下回る場合、その武器は発射不可（自滅させない）

### 継続ダメージ
- 被弾時、毎秒1ダメージを3秒間。被弾のたびに更新
- 最大累計3ダメージ
- 体力15以下では停止する（反撃不能のスパイラルを防ぐ）

---

## 3. 装備システム

### スロット構成
- ライン1 ×4 ／ ライン2 ×4（計8）
- **種別による配置制限なし。** 武器・シールド・設置・機動・隠密を自由に配置できる
- ラインの違いは「どちらの切替ボタンで順送りするか」のみ
- 各ラインに常に1つのアクティブスロットがある（＝2つが即応状態）
- カテゴリごとの所持数制限は設けない（シールドのみ各ライン1種まで）

### 枠コスト
```
{0, 3, 4, 5, 6, 7, 7, 8}  → 8スロット全埋めで最大体力 100 → 60
```

### 切替
- 各ライン1ボタン、順送りサイクル方式
- **直接選択は実装しない**（PCでも数字キー選択を作らない）
- 1回の切替は0.4秒。この間は攻撃不可
- 装備数が多いほど目的の装備への到達が遅くなる。これは仕様であり、
  ペナルティを別途加算しない
- 切替を挟んだ場合、リロードは最初からやり直し

### 「手」システム（設計の根幹）
プレイヤーは手を2本持つ。全ての装備は Handedness 属性を持つ。

| 属性 | 意味 |
|---|---|
| `"Two"` | 両手を占有。他の装備を一切使用できない |
| `"One"` | 片手を占有。片手装備同士なら2つ持てる |
| `"None"` | 手を占有しない。武器と併用できる |

**この1原則から全ての制約が導出される。個別の例外規定を追加しないこと。**

### 二丁持ち（片手装備2つ）
- 同時に発射することはできない。アクティブな手は常に1つ
- 持ち替えは0.15秒（通常切替0.4秒より高速）
- 利点は「切替の速さ」のみ。火力は増えない

理由：同時発射を許すと常時2倍火力かつ切替不要となり、
両手武器を選ぶ理由が消滅する。

### 同一武器の複数装備
- 弾薬は共有する（総弾数は増えない）
理由：弾薬が増えるとリロードが実質不要になり、「補給なし」の思想が崩れる

### UI要件
- 各スロットに色と記号でカテゴリを表示
- 記号は手の数と対応させる（■■=両手 / ■=片手 / それ以外=手を使わない）
- 装備数と最大体力の変動をリアルタイム表示する
  （空けられることが見えないと、プレイヤーは全スロットを埋めてしまう）
- 上限に達したカテゴリはグレーアウト

### 初版のロードアウト
プリセット3種（突撃型／斥候型／重装型）から選択。
自由編成UIは最終段階で実装する。

---

## 4. 武器

### 基本
- ラウンド中の弾薬補給なし
- 試合中の武器拾得は実装しない（ロードアウトで確定）
- 近接は独立ボタンを持たず、スロットの1つとして扱う

### 一覧（初版）
| 武器 | 手 | 威力 | 使用コスト | 備考 |
|---|---|---|---|---|
| アサルト | Two | 18 | 1/発 | 汎用 |
| ショットガン | Two | 12×8 | 4/発 | 近距離 |
| マークスマン | Two | 55 | 6/発 | 中距離 |
| スナイパー | Two | 60 | 3/発 | 下記参照 |
| ハンドガン | One | 22 | 0.5/発 | 切替が速い |
| 近接 | One | 45 | 0 | 最後の手段 |

### スナイパー
- **Handedness = "Two"。使用中は片手武器を含む全ての他装備を使用不可**
- 構えるまで0.8秒／**下ろすまで1.0秒（下ろす方を重くする）**
- 通常時 移動-10%／スコープ中 移動-60%
- **スコープ中はレーダー表示が消える**（事前照準を防ぐ・必須）
- 使用コストはあえて低め（3）。常時コストを既に払っているため罰を重ねない
- 胴体60（最低最大体力60でも即死しない）／頭部のみ即死
- 実効射程70m（対面スポーン間距離）

死に装備になるか否かはプレイヤーの練度に委ねる。
ただしマップ側には長射線を2〜3本必ず用意する（選択肢の存在保証）。

---

## 5. シールド

### 遮断仕様
- 敵の弾は遮る（貫通時も威力を軽減）
- 味方の弾は通す（背後から撃ち抜ける）
- **展開者本人は攻撃できない**

→ 展開シールド(3.5m)は「1人が身を捧げて味方の火力線を作る」装備になる

### 展開中の制約
- 攻撃不可
- **武器の切替不可（両ラインとも）**

### 種類
| 種別 | 幅×高 | 耐久 | 軽減 | コスト | 用途 |
|---|---|---|---|---|---|
| 集束 | 0.8×1.4 | 90 | 60% | 15 | 対狙撃・1対1 |
| 標準 | 1.2×1.5 | 40 | 40% | 12 | 汎用 |
| 展開 | 3.5×2.0 | 20 | 25% | 18 | 味方を守る |

### ダメージ計算
```
プレイヤー被害 = 元ダメージ × (1 - 軽減率)
シールド耐久 -= 元ダメージ
耐久0以下で消滅（その弾には軽減が適用される）
```

### 破壊時（制約は二重に課す）
1. **体力 -15 のペナルティ**
2. **再展開には一度別装備へ切り替える必要がある（カテゴリ単位）**
   - `RedeployScope = "Category"`。`"Slot"` にしないこと
   - 理由：スロット単位だと、片方が壊れた直後にもう片方を0秒で展開でき、
     切替を挟む制約が迂回される

### 所持数
- 各ライン1種まで（最大2枚）
- 上記の制約により連続展開はできず、複数持ちは実質的に自壊する

### 実装順
**標準シールド1種を完成させ、軽減計算・耐久処理・武器切替ロックが
正しく動作することを確認してから、集束・展開をパラメータ違いで追加する。**
3種を同時に作らないこと。

---

## 6. 支援装備

### 共通原則
- 手を占有しない（`Handedness = "None"`）
- 設置には体力を消費する
- 設置物はレーダーに表示される
- 設置者の死亡時に消滅する
- 設置は自分から一定距離内のみ。遠隔設置は実装しない

### バリケード
- 設置コスト25／**生成1.2秒・その間は移動不可**／持続10秒／耐久200
- 生成はキャンセル可能（コスト50%返却）
- 高さ1.8m・幅4.0m
- 硬直時間はテストプレイで調整する（初期値1.2秒）

**役割は「射線を切ること」であり「通行を阻むこと」ではない。**
通常ジャンプ(1.9m)で越えられるが、越える瞬間は無防備になる。

生成硬直が膠着防止の主機構である。交戦中には置けない。
「追い詰められてから壁を出す」は成立させない。

### 空中ジャンプ（設置物ではない／個人の機動力）
- 体力10を消費して空中でジャンプ。連続2回まで、着地でリセット
- 上昇量 約8m／着地後1.0秒は再使用不可
- 手を占有しないため、武器を構えたまま使用可

### 電磁迷彩
- `Handedness = "None"`。武器を使いながら潜伏できる
- レーダーから消える／目視では見える
- 起動コスト5／維持コスト4per秒
- 射撃で自動解除（解除後0.5秒は再起動不可）
- 試合開始後5秒は使用不可
- 体力20で強制解除

**常時全体レーダー環境下において、姿を消す唯一の手段である。**
実質的な必須装備となるが、これは許容する。
装備率が100%に張り付いた場合、消費コストではなく効果を弱める方向で調整する
（例：移動中は映る、静止時のみ消える）。

### 光学迷彩
- `Handedness = "Two"`。両手を占有するため移動しかできない
- 目視不可／レーダーには映る（別アイコンで表示）
- 維持コスト8per秒。解除に1.0秒
- 足音は消えない。高速移動時は輪郭が歪む
- **光学迷彩からの奇襲は成立しない**（解除に1秒かかるため）

### 攻撃する支援装備（投擲物・トラップ等）
初版では実装しない。将来追加する際は以下に従う。
- カテゴリ制限を適用しない
- 「1スロットあたり所持数1〜2個・試合中の補充なし」で制限する

理由：制限なしで追加すると、8スロット全てに投擲物を詰めて連投でき、
かつ手を占有しないため武器を構えたまま投げられる。

---

## 7. 空中状態の共通ルール（削除禁止）

接地していない間は常に以下が適用される。
通常ジャンプ・空中ジャンプ・落下中を問わない。

- **命中精度 -30%**
- **回避不可**

理由：ジャンプ高を1.9mに引き上げたため、これがないと
「飛び上がって遮蔽の上から撃つ」が最適解となり、
地上の位置取りとチーム連携が意味を失う。

---

## 8. レーダー

- **全プレイヤーの位置を常時表示**（敵味方とも）
- **2D座標のみ。高さ情報は表示しない**
- 電磁迷彩使用者は表示されない
- スナイパーのスコープ使用中は表示が消える

### 実装上の必須事項
**レーダー送信はサーバー側で電磁迷彩使用者を除外してから送ること。**
クライアント側での非表示処理は禁止（改造で丸見えになる）。

光学迷彩使用者は座標を送ってよい（レーダーに映る仕様のため）が、
モデルの可視化制御はサーバーからの状態通知に基づくこと。

高さを表示しない仕様は、本作で数少ない隠れた情報である。変更しないこと。

---

## 9. マップ

### 初版寸法
| 項目 | 数値 |
|---|---|
| 形状 | 円形・4方向対称 |
| 直径 | 80m |
| スポーン | 中心から35m・4箇所 |
| 中央交戦地帯 | 直径20m |
| 対面スポーン間 | 約70m ＝ スナイパー最大射程 |

**狭めから始め、実プレイで必要と判断された場合のみ広げる。**
理由：Robloxでは後から広げるのは容易だが、狭めるには遮蔽物の全面的な再配置が必要。

### 高さ基準（マップ着手前に厳守）
| 用途 | 高さ |
|---|---|
| 腰高遮蔽（しゃがんで隠れる） | 1.1m |
| 立ち遮蔽（立って隠れる） | 1.8m |
| 飛び越え可能な壁 | 1.8m 以下 |
| 飛び越え不可の壁 | 2.2m 以上 |
| 建物の階高 | 4.0m |

基準を先に確定してからマップ制作に入ること。
基準なしに作ると「登れる/登れない」の修正が全域に発生する。

### 移動
- 基本ジャンプ高 1.9m／着地後0.4秒は再ジャンプ不可
- 移動速度 約7m/秒（中心到達まで約5秒）

### 境界処理
- **見えない壁で物理的に阻む**（退場システムは実装しない）
- 接触時に薄いグリッドを一瞬表示し、バグではないと伝える
- 天井は想定最高到達点＋5m以上に置く
- 壁に阻まれても空中ジャンプの体力コストは返却しない

---

## 10. Bot

- 「両手武器1・片手武器1」の固定軽装のみ。切替判断をさせない
- 人数が足りない場合の穴埋めとして使用する
- 実装優先度は高い（人が集まらない期間を乗り切る唯一の手段）

---

## 11. 技術前提

- 言語：Luau (Roblox)
- **サーバー権威。** ダメージ・死亡・得点・勝敗は全て ServerScriptService で確定
- クライアントは入力送信と描画のみ
- 切替処理は武器専用として書かず、
  **「ラインのアクティブスロットを1つ進める」汎用処理**として実装すること

### 数値の扱い（厳守）
- 調整可能な数値をスクリプト内に直書きしない
- 必ず `ReplicatedStorage/GameConfig.lua` を参照する
- 理由：バランスは実プレイで反復調整するため、
  1回の調整コストを5分以内に抑える必要がある

### 命名規則
- サーバー：`SV_` ／ クライアント：`CL_`
- RemoteEvent：`RE_動詞_名詞`（例：`RE_Toggle_Stealth`）

---

## 12. 禁止事項

### 実装してはならない仕様
- リスポーン・蘇生
- ラウンド制・ラウンド先取
- 中央エリアの占拠とポイント加算
- エリア収縮
- 直接選択方式の装備切替
- 射撃による自滅
- 片手装備2つの同時発射
- クライアントからのダメージ値・体力値の送信
- 電磁迷彩使用者の座標をクライアントへ送ること

### 著作権に関する制約
ゲームのルール・システムに著作権は及ばない。以下のみを厳守する。

**使用禁止：**
- 既存作品由来の造語・固有名詞（組織名・エネルギー名・装備名）
- キャラクター名・部隊名
- 意匠（六角形のシールドパネル、隊服、エンブレム、配色、書体）
- 「◯◯風」といった既存作品名を用いた説明

**体力・エネルギーの呼称は一般名詞を用いること。**

---

## 13. 実装順序（飛ばさない）

| 段階 | 内容 | 目安 |
|---|---|---|
| 0 | GameConfig.lua の作成 | 1日 |
| 1 | 移動・射撃・ヒット判定（武器1種のみ） | 2週 |
| 1.5 | 体力消費型の射撃 | 1週 |
| 1.7 | 継続ダメージ | 0.5週 |
| 2 | Bot（固定軽装） | 2週 |
| 3 | チーム分け・得点集計・終了判定 | 2週 |
| 4 | レーダーUI（サーバー側フィルタ込み） | 2週 |
| 4.5 | ライン切替（汎用処理として実装） | 2週 |
| 4.7 | スナイパー（両手武器の検証） | 1週 |
| 5 | 3チーム同時成立 | 3週 |
| 6 | バリケード（設置系の基礎実装） | 1.5週 |
| 6.5 | 標準シールドのみ | 2週 |
| 6.8 | 集束・展開シールド（パラメータ追加） | 0.5週 |
| 6.9 | 空中ジャンプ | 1週 |
| 7 | 電磁迷彩 | 2週 |
| 8 | 観測モード＋Ping | 2週 |
| 9 | 光学迷彩＋プリセット選択UI | 3週 |
| 10 | 自由編成UI・8スロット全対応 | 3週 |

### 段階1の注意
基本ジャンプ高1.9mと空中ペナルティを**最初から**入れること。
後付けすると全ての操作感が変わり、それまでの調整が無駄になる。

---

## 14. 未確定・要調整項目

実プレイで計測し、判断する。

| 項目 | 現行値 | 調整の兆候 |
|---|---|---|
| バリケード生成硬直 | 1.2秒 | 誰も使わない／簡単すぎる |
| 電磁迷彩 維持コスト | 4/秒 | 装備率が100%に張り付く |
| マップ直径 | 80m | 平均試合時間が90秒未満／接触が遅い |
| スポーン半径 | 35m | 開幕の交戦が早すぎる |
| 生存ボーナス | 2点 | 最後まで隠れる者が出る |
| 全体の枠コスト | -40（8枠時） | 軽装一択になる／重装一択になる |
